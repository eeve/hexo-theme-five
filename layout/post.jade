extends base

block site_meta
	if page.title
		title #{page.title} - #{config.title}
	else
		title #{config.title} - #{config.subtitle}

block site_body
	!= partial('_partial/article', { post: page })
